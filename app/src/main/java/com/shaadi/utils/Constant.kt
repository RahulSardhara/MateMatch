package com.shaadi.utils

object Constant {

    const val DATABASE_NAME = "match_mate_db"
    const val USER_TABLE = "users"
    const val IS_IMAGE_LEGAL_POLICY = false
    enum class UserListType { ALL, ACCEPTED, REJECTED,UNKNOWN }
}